apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  labels:
    app: appointment-service
spec:
  selector:
    app: appointment-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 4001
  type: ClusterIP
